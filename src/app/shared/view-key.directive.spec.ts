import { ViewKeyDirective } from './view-key.directive';

describe('ViewKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewKeyDirective();
    expect(directive).toBeTruthy();
  });
});
